<UserControl x:Class="SRKT.WPF.Views.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Przegląd Dnia: " FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="{Binding DzisiejszaData, StringFormat=d}" FontSize="24" FontWeight="Bold" Foreground="#3498db"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding DzisiejszeRezerwacje}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Godzina" Binding="{Binding GodzinaRozpoczecia, StringFormat={}{0}:00}" Width="80"/>
                <DataGridTextColumn Header="Kort" Binding="{Binding KortId}" Width="80"/>
                <DataGridTextColumn Header="Klient (ID)" Binding="{Binding UzytkownikId}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding CzyOplacona}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>